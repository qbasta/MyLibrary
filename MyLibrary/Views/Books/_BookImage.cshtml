@model MyLibrary.Models.Book

@{
    var imgText = "";
    if (Model.Image.EndsWith("NoImage.png"))
    {
        imgText = "No photo for " + Model.Name;
    }
    else
    {
        imgText = "Cover of " + Model.Name;
    }
}


<img src="~/images/@Model.Image" class="image-fluid" alt="@imgText" title="@imgText" />