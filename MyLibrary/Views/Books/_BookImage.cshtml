@model MyLibrary.Models.Book

@{
    var imgText = "";
    if (Model.Image.EndsWith("cover0.png"))
    {
        imgText = "No photo for " + Model.Name;
    }
    else
    {
        imgText = "Cover of " + Model.Name;
    }
}


<img src="@Model.Image" class="image-fluid" alt="@imgText" title="@imgText" />